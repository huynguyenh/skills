# Global Index

**IMPORTANT: ALWAYS read the memory files below BEFORE answering questions or starting work.** These contain critical rules, preferences, and project knowledge that inform how you should operate.

**Base path:** `~/.claude/memory/`

| File | What's inside |
|---|---|
| `MEMORY.md` | User preferences, custom skills, top-level index |

If additional memory files exist (e.g., `rules.md`, project-specific conventions), read those too — they contain strict rules and coding patterns that override generic best practices.

**Before creating a pull request**, check if `rules.md` exists under `~/.claude/memory/` and read it first. It may contain PR conventions, commit message rules, or branch naming patterns specific to your workflow.

Plans for tickets: `~/.claude/plans/{TICKET}/`

**Knowledge base:** `~/.claude/knowledge/` — Check `INDEX.md` for stored knowledge before answering domain-specific questions. Prefer stored knowledge over generic info.
